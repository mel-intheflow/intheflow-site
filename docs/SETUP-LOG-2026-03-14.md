# Setup Log — InTheFlow CMS + Beta Automation (2026-03-14)

## Ziel
Design unverändert lassen, aber Textpflege für Melanie stark vereinfachen über CMS + automatischen Deploy-Flow.

## Umgesetzt

### 1) Repository & Basis
- Repo eingerichtet: `mel-intheflow/intheflow-site`
- Bestehende Website in Repo übernommen
- Beta-Konzept gewählt: Live bleibt unberührt, Arbeit auf `/beta`

### 2) Beta Deployment (FTP)
- Website nach `/beta` deployt
- Rechteprobleme bei Bildern behoben (`755` Verzeichnisse, `644` Dateien)
- Beta erreichbar:
  - `/beta/`
  - `/beta/admin/`

### 3) Decap CMS integriert
- Dateien angelegt:
  - `admin/index.html`
  - `admin/config.yml`
- Backend auf `git-gateway` umgestellt
- Netlify Identity + Git Gateway als Auth-Layer verwendet
- Login-Fixes durchgeführt (Redirect/URL/Config)

### 4) Content-Entkopplung (Design bleibt gleich)
- `content/home.json` als Content-Quelle
- `assets/js/content-loader.js` lädt Inhalte in bestehende HTML-Blöcke
- Layout/CSS unverändert, nur Textquellen modular

### 5) Full Field Mapping (V2)
- Deutlich erweitertes Mapping fast aller Homepage-Textblöcke
- Repeater-Listen für Kartenbereiche (Offer/Courses/Events)
- CTA/Badge/Kontakt-Quote/Status- und Impulsblöcke abgebildet
- Feldübersicht dokumentiert in `docs/CMS-FELD-MAP.md`

### 6) Automatischer Deploy GitHub -> FTP
- GitHub Action angelegt:
  - `.github/workflows/deploy-beta-ftp.yml`
- Trigger: Push auf `main`
- Ziel: automatische Veröffentlichung nach `/beta/`
- GitHub Secrets gesetzt:
  - `FTP_SERVER`
  - `FTP_USERNAME`
  - `FTP_PASSWORD`

## Ergebnis
- Melanie kann im CMS Inhalte pflegen
- Änderungen gehen in GitHub
- Deploy nach `/beta` läuft automatisch
- Design der Website bleibt erhalten
- Live-Seite bleibt unangetastet, bis bewusster Switch erfolgt

## Offene Punkte (nächster Schritt)
1. Feinschliff einzelner Restfelder (falls im Editor noch Lücken auffallen)
2. Optional: Workflow für `/` Live-Switch definieren (Backup + Promote von `/beta`)
3. Optional: kleines „Editor Quickstart“ für Melanie
